import 'package:flutter/material.dart';

const Color black = Colors.black;
const Color black87 = Colors.black87;
const Color white = Colors.white;
const Color grey = Colors.grey;
const Color teal = Colors.teal;

///

const buttonsTextStyle = TextStyle(
  color: white,
  fontWeight: FontWeight.bold,
  fontSize: 12,
);

const productTitleTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
  fontSize: 18,
  letterSpacing: 0.5,
);

const productDescTextStyle = TextStyle(
  fontWeight: FontWeight.normal,
  fontSize: 13,
);

const priceTextTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
  fontSize: 13,
);

const priceTextStyle = TextStyle(fontWeight: FontWeight.normal, fontSize: 13);

const ratingTextStyle = TextStyle(
  color: white,
  fontWeight: FontWeight.bold,
  fontSize: 12,
);

const categoryTextStyle = TextStyle(
  color: teal,
  fontWeight: FontWeight.bold,
  fontSize: 12,
);

BoxDecoration decoGrey = BoxDecoration(
  color: grey,
  borderRadius: BorderRadius.circular(4),
);

const pCategory = EdgeInsets.fromLTRB(2.0, 12.0, 2.0, 0.0);
const pPage = EdgeInsets.all(12.0);
const pPrice = EdgeInsets.symmetric(vertical: 7.0);

const pTitle = EdgeInsets.symmetric(vertical: 5);
const pImage = EdgeInsets.all(5);
const pButton = EdgeInsets.all(6);
const pButtonCircle = EdgeInsets.all(10);

const pRating = EdgeInsets.all(3);
